# ── Core metadata extraction ───────────────────────────────────────
pypdf>=3.0.0              # PDF reading: metadata dictionary, XMP stream, encryption status
python-docx>=1.0.0        # Word DOCX reading: core_properties, document structure
Pillow>=10.0.0            # Image opening: format, DPI, JPEG quantization tables
exifread>=3.0.0           # JPEG/TIFF EXIF extraction: camera, GPS, timestamps

# ── API layer (optional — needed only when exposing as a web service) ─
fastapi>=0.110.0          # REST API framework
uvicorn[standard]>=0.29.0 # ASGI server to run FastAPI
python-multipart>=0.0.9   # Required for FastAPI file upload (UploadFile)
